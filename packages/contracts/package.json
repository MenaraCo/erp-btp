{
  "name": "@erp-btp/contracts",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"(contracts) no lint yet\"",
    "test": "echo \"(contracts) no tests yet\""
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
